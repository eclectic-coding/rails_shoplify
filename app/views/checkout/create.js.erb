var stripe = Stripe("<%= Rails.application.credentials[Rails.env.to_sym][:stripe][:publishable_key] %>");

stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>'
  }).then(function (result) {
  console.log(result.error.message);
});